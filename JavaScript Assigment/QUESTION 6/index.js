//06. 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon' split the string at the comma and change it to an array.

const str1 = 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon';

const arr = str.split(","); // Splitting based on Comma

// Output this Program

console.log(arr);